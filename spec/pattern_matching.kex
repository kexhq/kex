# Pattern matching
# Expected output:
# the answer
# B
# 30
# hello

main do
  let result = match 42 do
    0 -> "zero"
    42 -> "the answer"
    _ -> "other"
  end
  IO.printLine(result)

  let grade = match 85 do
    n when n >= 90 -> "A"
    n when n >= 80 -> "B"
    n when n >= 70 -> "C"
    _ -> "F"
  end
  IO.printLine(grade)

  let (a, b) = (10, 20)
  IO.printLine(a + b)

  let [first | rest] = ["hello", "world"]
  IO.printLine(first)
end

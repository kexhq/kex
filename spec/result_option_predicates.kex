# ok?/error? (Result<T, E> = Ok(T) | Error(E)) and present?/none?
# (T? = Just(T) | None) — predicates implemented by the source-owned
# Optional and Result make blocks in optional.kex.
main do
  IO.printLine(Ok(1).ok?)
  IO.printLine(Ok(1).error?)
  IO.printLine(Error("bad").error?)
  IO.printLine(Error("bad").ok?)
  IO.printLine(Just(1).present?)
  IO.printLine(Just(1).none?)
  IO.printLine(None.none?)
  IO.printLine(None.present?)
end

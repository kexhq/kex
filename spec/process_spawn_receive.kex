# kex: run-beam
# spawn + receive round-trip using |sender| binding.

type Msg = Ping | Pong

main do
  let server = spawn do
    receive do |sender|
      Ping => sender.send(Pong)
    end
  end
  server.sendFrom(Ping)
  receive do
    Pong => IO.printLine("pong received")
  end
end

let explain(n: Integer) -> String do
  match n do
    x when x < 0 => "negative"
    0            => "zero"
    _            => "positive"
  end
end

main do
  IO.printLine(explain(-5))
  IO.printLine(explain(0))
  IO.printLine(explain(5))
end

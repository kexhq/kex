let describe(n: Integer) -> String do
  match n do
    x when x < 0 -> "negative"
    0            -> "zero"
    _            -> "positive"
  end
end

main do
  IO.printLine(describe(-5))
  IO.printLine(describe(0))
  IO.printLine(describe(5))
end
